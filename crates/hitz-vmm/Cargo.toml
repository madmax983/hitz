[package]
name = "hitz-vmm"
description = "VM lifecycle, vCPU threads, memory management"
version.workspace = true
edition.workspace = true
license.workspace = true

[lints]
workspace = true

[dependencies]
hitz-hal.workspace = true
hitz-boot.workspace = true
thiserror.workspace = true
tracing.workspace = true

# Phase 2
hitz-devices.workspace = true

# Phase 5

# Phase 7
tokio.workspace = true
hitz-net.workspace = true

# Phase 12 — vsock channel types in BootExtras / VsockIoHandle
crossbeam-channel.workspace = true

# Phase 11 — thin OTel API only (no SDK)
opentelemetry.workspace = true

[target.'cfg(windows)'.dependencies]
windows = { workspace = true, features = ["Win32_System_Memory"] }

[dev-dependencies]
loom = "0.7.2"
proptest = "1.11.0"
tempfile = "3"
