--- memory.rs
+++ memory.rs
@@ -137,8 +137,7 @@
         for existing in &self.regions {
             let existing_end = existing
                 .gpa_start
                 .as_u64()
-                .checked_add(existing.size as u64)
-                .unwrap_or(u64::MAX);
+                .saturating_add(existing.size as u64);
             if gpa.as_u64() < existing_end && gpa_end > existing.gpa_start.as_u64() {
