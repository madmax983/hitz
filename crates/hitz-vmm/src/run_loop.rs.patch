--- crates/hitz-vmm/src/run_loop.rs
+++ crates/hitz-vmm/src/run_loop.rs
@@ -619,7 +619,7 @@
         };
         handle_io_port(&mut vcpu, &mut serial, &io).expect("handle_io_port should succeed");
         assert_eq!(vcpu.regs.rip, 102);
-        assert_eq!(serial.writer(), b"A".as_ref());
+        assert_eq!(serial.writer(), &vec![b'A']);
     }

     #[test]
