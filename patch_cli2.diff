--- crates/hitz-cli/src/main.rs
+++ crates/hitz-cli/src/main.rs
@@ -1746,9 +1746,9 @@
             break;
         }

-        if args.duration_secs.is_some_and(|duration| start_time.elapsed().as_secs() >= duration) {
+        if let Some(duration) = args.duration_secs.filter(|&d| start_time.elapsed().as_secs() >= d) {
             println!(
                 "{}",
-                format!("⏹ Recording reached duration limit ({}s).", args.duration_secs.unwrap()).yellow()
+                format!("⏹ Recording reached duration limit ({duration}s).").yellow()
             );
             break;
