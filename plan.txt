1. Rewrite `format_error_response` in `crates/hitz-cli/src/main.rs`.
   - Prevent it from outputting raw JSON by checking for unrecognized JSON and extracting key-value pairs instead.
   - If `serde_json::from_str::<serde_json::Value>(resp)` succeeds, output the extracted pairs (like `key: value, key2: value2`).
2. Replace occurrences of `println!("{}", format!("✗ Failed to parse ...: {resp}").red())` in `handle_vm_list` and `handle_vm_status` with `print_error_response(status, &resp, "Failed to parse ...")`. Wait, `status` is 200 OK.
   Actually, `format_error_response` expects `hyper::StatusCode`. If we call it with `status` (which is 200 OK), it will say `(200 OK): {resp}`. That's fine.
3. Complete pre commit steps to ensure proper testing, verification, review, and reflection are done.
4. Submit the change.
