💡 **The Spark:** "I noticed we calculate efficiency and carbon footprints but don't provide actionable advice to users."
🚀 **The Feature:** "Implemented the `Advisor` trait for `MetricsSnapshot` to recommend scaling actions based on efficiency scores and estimate the carbon savings."
🔮 **The Potential:** "Users can directly action these recommendations to optimize resource usage, saving money and reducing their carbon footprint."
⚠️ **Risk:** "Low. The feature is completely isolated in `crates/hitz-api/src/advisor.rs` and gated behind an `advisor` feature flag."
