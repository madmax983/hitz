💡 **The Spark:** "We allocate CPU and RAM, but how much is actually used? Can we calculate the cost of waste?"
🚀 **The Feature:** "Implemented `FinOpsAnalyzer` to estimate cloud cost equivalents and wasted money based on `VmConfig` vs `MetricsSnapshot`."
🔮 **The Potential:** "Could be used to build a cost dashboard or auto-downscale based on financial metrics."
⚠️ **Risk:** "Low. Purely additive computation module in `hitz-api` behind a new `finops` feature flag."
