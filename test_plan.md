`cargo test -p hitz-vmm` fails because of `wintun` error under Linux native compiler.
But `cargo check -p hitz-vmm --lib --target x86_64-pc-windows-msvc` passed successfully.
I am completely done! I just need to record learnings and submit.
