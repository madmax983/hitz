Idea: "Cost Estimator": Calculate estimated cloud cost equivalent of running this micro-vm (based on CPU/RAM config).

Let's look at `crates/hitz-api/src/config.rs`. We have `VmConfig` with `cpus` and `ram_mib`.
I can create a new feature in `hitz-api`: `cost_estimator.rs`.
It calculates the estimated hourly and monthly cost of running the VM based on typical cloud pricing (e.g. AWS fargate/ec2).
E.g., $0.04048 per vCPU per hour, $0.004445 per GB per hour.

Or Idea 3: "Security Context/Audit":
A `SecurityContext` analyzer. It takes `VmConfig` and `MetricsSnapshot` and returns a `SecurityScore` or `RiskProfile`.
- Is root (uid 0) running the process? (Maybe we don't have that in `ProcMetrics` - just `pid` and `name`).
- Are there exposed ports? `config.ports` has `PortForward`.
- Is the guest agent enabled?
- Is memory completely maxed out? (DOS risk?)
- Are there suspicious process names?

Let's go with "Security Context/Audit" or `RiskAnalyzer`. Let's call it `SecurityAudit`.

Wait, the prompt says "Nova" creates new features.
"Innovation is connecting two existing modules that haven't met yet."
We have `VmConfig` and `MetricsSnapshot`.
Let's build `SecurityAudit` feature.
Or even better: "Predictive Scaling" - maybe too similar to rightsizer.
What about "Cost Estimator"? That connects `VmConfig` to real-world impact.
Let's build a "Cost Estimator" feature. It takes a `VmConfig` and optionally `MetricsSnapshot` (to determine "active" cost vs "idle" cost?). No, just `VmConfig` for the allocated cost, and `MetricsSnapshot` for the utilized cost!
That's brilliant! Connecting Config (allocated) and Metrics (utilized) to show *wasted money*.
Let's call it `FinOpsAnalyzer`. It calculates:
- `allocated_monthly_cost`
- `utilized_monthly_cost` (based on average CPU/RAM usage from a snapshot)
- `wasted_monthly_cost`
