I will fix the failing execution of tests for `crates/hitz-api/src/api.rs`, specifically for tests that verify the representation.

I will also modify `crates/hitz-guest-agent/src/main.rs` to write unit tests for the missing code sections, such as for the internal functions:
- `read_uptime_secs()`
- `parse_load_avg()`
- `send_snapshot()`
- `push_loop()`
- `pull_server()`
- `now_ms()`

I will also make sure to use `#[cfg(test)] mod tests` block, use `assert!` and descriptive naming.

I will run `cargo llvm-cov` to ensure my coverage increased and execute all workspace tests.

I will now submit the PR.
