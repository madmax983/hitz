I have successfully implemented the optimizations in the `hitz-vmm` and `hitz-daemon` crates. Let's make sure the plan is well-documented and executed properly.

1.  **Optimization in `hitz-daemon/src/router.rs`**: Replaced `.clone()` and `.to_string()` for `req.method()` and `req.uri().path()` with references bound directly to the request object. This eliminates heap allocations per request on the HTTP router hot path.
2.  **Optimization in `hitz-vmm/src/run_loop.rs`**: Simplified the `Mutex` lock handling in `poll_devices()` and `dispatch_exit()` for `SharedDevices`, avoiding early drops and warning `clippy::significant_drop_tightening`, streamlining execution on the hot vCPU run loop.
3.  **Optimization in `hitz-vmm/src/vm.rs`**: Refactored the `run_single_vcpu` and `run_multi_vcpu` method signatures to accept references instead of cloned Arcs. This removes multiple `.clone()` heap allocations when creating the vCPU runner threads. We also replaced `std::io::Error::new()` with `std::io::Error::other()` directly.
4. **Validation**: We ran `cargo check`, `cargo fmt`, and `cargo clippy`. `wintun` fails to cross-compile due to upstream crate limitations with the target ABI when building on Linux, which is a known issue. We verified with `clippy --lib -p hitz-vmm` and `cargo check --lib -p hitz-daemon`.
