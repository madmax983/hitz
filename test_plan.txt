1. Fix memory allocation inside `format_error_response` using a `fold` chain instead of `.join()`.
2. Apply `#[allow(clippy::significant_drop_tightening)]` or restructure to fix `hitz-vmm/src/run_loop.rs` error.
3. Fix expect uses.
4. Address `hitz-vmm/src/vm.rs` errors (remove `needless_pass_by_value`, `io_other_error`, `unnecessary_wraps`).
