1. We need to add tests for `hitz-api/src/api.rs` to cover the `Display`, `gerund()`, and `past_tense()` implementations of `VmAction` (lines 68-98).
2. We need to add tests for `hitz-guest-agent/src/main.rs`, specifically the `parse_proc_pid_stat` function (lines 164-200) to cover branches that are currently untested.
3. We will run code coverage again to see the impact.
